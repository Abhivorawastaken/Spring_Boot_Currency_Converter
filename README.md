# Spring_Boot_Currency_Converter
This project uses Spring Boot to build REST APIs, integrates an external exchange rate API using RestTemplate, stores conversion data in PostgreSQL via Spring Data JPA, and includes validation, logging, caching, and Actuator monitoring for production-ready backend functionality.
